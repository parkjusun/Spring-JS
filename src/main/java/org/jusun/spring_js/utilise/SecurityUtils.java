package org.jusun.spring_js.utilise;import java.security.MessageDigest;import java.security.NoSuchAlgorithmException;public class SecurityUtils {    private static String salt = "spemcvhj12sdfsmssSEVVeqsz";    // Salt와 비밀번호를 결합하여 해시 생성    public static String encodeSHA256WithSalt(String input) {        try {            // SHA-256 인코딩            MessageDigest digest = MessageDigest.getInstance("SHA-256");            String inputWithSalt = input + salt;            byte[] hash = digest.digest(inputWithSalt.getBytes());            // 해시 값을 16진수 문자열로 변환            StringBuilder hexString = new StringBuilder();            for (byte b : hash) {                String hex = Integer.toHexString(0xff & b);                if (hex.length() == 1) {                    hexString.append('0');                }                hexString.append(hex);            }            return hexString.toString();        } catch (NoSuchAlgorithmException e) {            throw new RuntimeException(e);        }    }}